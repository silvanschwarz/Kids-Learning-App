import styled from 'styled-components/native';
import { ButtonNextLetter } from 'src/components/button';

const WrapButtonNextLetter = styled(ButtonNextLetter)`
  margin-bottom: 65px;
  margin-top: 35px;
`;

export { WrapButtonNextLetter };
